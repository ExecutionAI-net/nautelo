# NAUTA — Multi-Phase Delivery Tracker

Cross-phase status board for the spec's 24 delivery phases (see
`NAUTA_PRODUCTION_IMPLEMENTATION_SPEC.md` §7 for the full dependency table).
Each phase still gets its own plan file (`docs/superpowers/plans/`) and its
own SDD ledger (`.superpowers/sdd/<plan-name>/progress.md`) once execution
starts — this file is just the bird's-eye view across all of them, updated
by the controller at each wave transition.

Strategy (per project owner's instruction, 2026-09-17): maximize safe
parallelism — plan ahead of the current wave, run independent phases
concurrently (per the dependency table, not just adjacent phase numbers),
never merge a PR without green CI and a clean/conflict-free merge state.

## Status legend
`idea` → `planning` → `plan-review` → `implementing` → `final-review` → `done`

## Phase status

| Phase | Name | Depends on | Status | Plan file | Notes |
|---:|---|---|---|---|---|
| 0 | Repository audit and scope freeze | — | done | (folded into Phase 0/1 plan) | |
| 1 | Infrastructure and environments | 0 | done | `2026-09-17-phase-0-1-infrastructure.md` | Merged in full 2026-09-17. |
| 2 | Shared domain types and platform settings | 1 | **done** | `2026-09-17-phase-2-shared-types-platform-settings.md` | All 6 tasks merged (PR #21, #25, #27). |
| 3 | Identity, organizations and permissions | 2 | **done** | `2026-09-17-phase-3-identity-organizations-permissions.md` | **Fully complete.** All 12 tasks + a final whole-branch review + its one fix-forward task (PR #57 — production CORS had never been configured, a deploy blocker; the 404 error envelope was degrading to a generic code) all merged to `dev`. 4 full plan review/fix rounds before implementation, every task independently reviewed after, catching several real privilege-escalation-adjacent bugs along the way. Two real frontend bugs found during Task 12's walkthrough (verify-email double-submit UX bug; concurrent refresh-token race) deferred as standalone follow-up task chips, not blocking. See ledger for full detail. |
| 4 | Brand/model taxonomy | 3 | **done** | `2026-09-17-phase-4-brand-model-taxonomy.md` | All 8 tasks merged (PR #22, #23, #26). |
| 5 | Directory consolidation | 3, 4 | **done** | `2026-09-18-phase-5-directory-consolidation.md` (merged, PR #34) | **Fully complete.** 17 tasks, ~5780 lines. All 17 merged (PR #69-74, #76-78, #81, #83-89) + a final whole-branch review + its one fix-forward task (PR #91) all merged to `dev`. Task 10 (the spec §14.3 `import_legacy_directory` management command) needed one fix round for a real idempotency bug (a staff title rename after import could duplicate a `ProfessionalService` row on reimport), independently re-verified via mutation testing before merge. The final whole-branch review found no Critical issues; its blocking findings were closed by the fix-forward (stale tracker/activity records, a false spec-coverage claim now backed by a real Phase 6 seam comment on the directory page, a technically wrong `page.tsx`-vs-redirect rationale in the Contract summary, a vacuous-pass-prone contact-field test, a related-professionals queryset that ignored the peer category's own `is_active` state and lacked an ordering tiebreak, two missing contract tests — a URL-conf gating/throttle sweep and the first test coverage for `lib/api/directory.ts`'s `null`-vs-empty contract — and the missing directory pagination controls). Two findings were deliberately spun off rather than patched here: the SSR client-IP forwarding gap (all visitors currently share one `services_directory` throttle bucket — security-sensitive, now a written Known Limitation) and a batch of minor cleanups. Extensive "Contract summary for later phases" (12 rules) and "Known Limitations" list in the plan doc — required reading before Phases 6, 7, 15, 17, 19 and 20 touch the directory. See ledger for full detail. |
| 6 | Shared inquiry and messaging core | 3, 5 | **done** | `2026-09-18-phase-6-inquiry-messaging.md` (merged, PR #116) | 15 tasks. Three review rounds (R1: 4 Critical / 6 Important; R2: 2 Critical / 1 Important; R3 READY). New `messaging` + `notifications` apps, the §15.3 atomic inquiry transaction, guest-draft token, idempotent notifications, frontend inquiry form. Contract rule 11a: the feature-flag gate goes FIRST in `permission_classes` on every messaging view (Phase 7 inherits it). **READY TO IMPLEMENT.** **Implemented, all 15 tasks merged (PR #141-#193).** Contract summary is required reading before Phases 7, 18 and 19 touch messaging. Deferred: messages page (19/20), WebSocket (18). |
| 7 | Contact privacy and reveal access | 6 | **done** | `2026-09-18-phase-7-contact-privacy.md` (merged, PR #121) | 12 tasks. Three review rounds (R1: 4 Critical / 5 Important; R2: 2 Critical / 2 Important; R3 READY). Masking, `ContactAccessService`, public reveal endpoint (`Cache-Control: private, no-store` + `Vary`), staff revoke, adversarial leak sweep, session-aware `ContactPanel`, InquiryForm refresh seam. Ruled: reveal endpoints carry no flag gate (`contact_unlock` only closes reveal); Phase 6 rule 11a carve-out landed in #120. Depends on Phase 6's `messaging` app being merged. Phase 6 dependency is now done; read its Contract summary. **Implemented, all 12 tasks merged.** |
| 8 | Finance configuration and calculation engine | 2 | **done** | `2026-09-17-phase-8-finance-calculation-engine.md` | All 8 tasks merged (PR #19, #20, #24). Formula independently cross-verified against spec twice. First phase (besides 0/1) fully complete. |
| 9 | Finance UI and broker listing flag | 4, 8, 11 (see note) | **done** | `2026-09-18-phase-9-finance-ui.md` (merged, PR #97) | 12 tasks. Two full review→fix→re-review cycles, cleared for execution. A real correctness bug found in already-merged Phase 11 code (`_clean_percent` accepted a 100% down-payment override, spec §17.3 requires 0-99.99%) was pulled out into its own standalone, already-merged prerequisite fix (PR #95) rather than bundled here. Exposes the active finance configuration's defaults via the public settings endpoint (composed at the view layer to avoid a real cache-staleness bug found during planning) so the standalone `/financing/` calculator can pre-fill with real values. **READY TO IMPLEMENT.** |
| 10 | Listing view analytics | 3, 4, 11 (see note) | **done** | `2026-09-18-phase-10-listing-analytics.md` (merged, PR #93) | 8 tasks. Two full review→fix→re-review cycles, cleared for execution. `common.ip` fixes a real, independently-discovered DRF throttle-identity vulnerability project-wide (unconfigured `NUM_PROXIES` trusts the entire client-supplied `X-Forwarded-For` header — a full rate-limit bypass via header rotation), safely (default preserves today's behavior for legitimate traffic). Also adds an off-by-default IPv6 prefix-truncation setting closing a view-count-inflation vector. **READY TO IMPLEMENT.** |
| 11 | Listing workflow and immutable snapshots | 3, 4 | **done** | `2026-09-18-phase-11-listing-workflow.md` (merged, PR #33) | **Fully complete.** All 15 tasks + a final whole-branch review + its one fix-forward task (PR #67 — 4 missing authz/feature-flag tests for submit/withdraw; Django admin hardened against an unaudited write path into immutable fields; a real behavior fix closing a path where a suspended/expired listing could mutate its own live public snapshot) all merged to `dev`. 7 end-to-end acceptance tests proved spec §20's definition of done, §40 Scenario I and §34.2's concurrency requirement with zero production-code changes needed on first attempt. Several tasks adversarially security/leak-tested (Task 12's staff decision endpoint, Task 14's public read API — explicitly tested for pending-listing leaks, none found). Extensive "Contract summary for later phases" and 15-item "Known Limitations" list in the plan doc — required reading before Phases 9/10/12/13/14/15/16/17/18/20/21 touch `listings/`. See ledger for full detail. |
| 12 | Broker policy and auto-approval | 11 | **done** | `2026-09-18-phase-12-broker-policy.md` (merged, PR #94) | 10 tasks. Two full review→fix→re-review cycles, cleared for execution. Extracts a shared `publish_revision` from Phase 11's decision path, adds the auto-approval submit branch, an audited policy-toggle service, staff broker read/write endpoints, bulk-approval, and a staff broker detail screen. A Critical finding (submit endpoint returned `"revision": null` on the auto-approval path, breaking spec §30.2's "mutations return updated resource" contract) was fixed and independently re-verified against Django's `refresh_from_db()` semantics. `submissions.py`'s `submit_listing_revision` is also touched by Phase 13's plan — Task 2 must fully merge before Phase 13's corresponding task begins, not run concurrently. **READY TO IMPLEMENT.** |
| 13 | Individual quota and entitlement ledger | 11 | **done** | `2026-09-18-phase-13-quota-entitlement.md` (merged, PR #99) | 14 tasks. Three review→fix→re-review cycles (deepest of the four new phase plans), cleared for execution. New `entitlements` app, `UserEntitlement` model/ledger, rolling-window eligibility service, submission-flow gating, expiry sweep, staff operations. A Critical finding (the free-quota gate's pre-check ran above `consume()`'s own correct short-circuit, permanently blocking resubmission of a withdrawn listing or any post-publication edit once a seller's one free right was consumed — a real regression on a working Phase 11 flow) was fixed by guarding the call site rather than the gate's signature. A real Phase 11 gap surfaced but correctly left out of scope: correcting a withdrawn/rejected pre-publication listing currently requires resending its entire payload, not just the corrected field (Known Limitation 16, likely Phase 16's to fix). `submissions.py`'s `submit_listing_revision` is also touched by Phase 12's plan — Phase 12's Task 2 must fully merge before Phase 13's corresponding task begins, not run concurrently. **READY TO IMPLEMENT.** |
| 14 | Stripe product and fulfillment | 13 | **done** | `2026-09-18-phase-14-stripe-products-fulfillment.md` (merged, PR #143; implemented PRs #172-#178+) | 13 tasks, backend-only (`payments` app). Two independent review rounds (R1: 3 Critical / 6 Important; R2: 1 Important fixed). Official Stripe SDK signature verification over the raw body, dedupe row + fulfillment in one transaction, exactly-once DB backstop (`entitlements_one_live_right_per_payment`), refunds/disputes, return-URL allowlist, no-network tests. Implemented in full, backend-only; flag stays off and products inactive until staff supply Stripe ids. Deviations: `verify_stripe_event` returns a plain dict; row locks use `of=("self",)`. |
| 15 | Media policy and processing | 13, 14 | **backend done** | (no plan doc) | Upgrade apply, upload pipeline, file policy merged. Seller upload UI shipped in Phase 16; upgrade modal, Pillow/ffmpeg/scanner not yet added. |
| 16 | Role-aware create/edit experience | 9, 11-15 | **partial** | (no plan doc) | `/sell/` form (brand/model/Other, immutable fields locked after first publication) with intent/upload/complete media flow and submit. Also: media upgrade panel (apply or buy via Checkout) and broker `/fleet/`. Also: broker finance fields, `/dashboard/listings/` (mine) and `/sell/<id>/` editing via `listings/mine/` and `listings/<id>/workflow/`. Form copy is EN/IT/ES (`lib/i18n/sell.ts`); the media panel and staff screens are still English only. |
| 17 | Staff products, moderation and taxonomy UI | 12-16 | **partial** | (no plan doc) | Backend complete. Frontend: moderation queue (`/dashboard/staff/`) and revision review with approve/request-changes/reject. Other-model queue with map / create-and-map at `/dashboard/staff/taxonomy/`. Also: suspend/reinstate from the queue and entitlement ledger (`/dashboard/staff/entitlements/`). Not built: brand/model admin and merge UI. |
| 18 | WebSocket, email and in-app notifications | 6, 11-17 | **partial** | (no plan doc) | Backend complete; frontend bell (polling inbox, mark read/all, EN/IT/ES copy). WebSocket client with polling fallback; per-user email opt-out (`notifications/preferences/`). Not built: scheduled staff digest. |
| 19 | Broker dashboard simplification and messages | 6, 12 | **done** | `2026-09-18-phase-19-broker-dashboard-messages.md` (merged, PR #144) | 11 tasks (53 backend + 121 frontend new tests). Two review rounds; EN/IT/ES dictionary for the dashboard nav. Phase 6 dependency is now done; read its Contract summary. **Implemented, all 11 tasks merged (PRs #205-#215).** Recipient-only archive, broker dashboard metrics endpoint, role-neutral `components/messages/` mounted at `/dashboard/messages/` and `/dashboard/broker/messages/`, 301 for the retired services route. Carry-forward: Phase 20 deletes `NAVIGABLE_URL_PREFIXES`. |
| 20 | Public card/profile integration and responsive QA | 5-10 | **partial** | (no plan doc) | Immutable listing slug + `listings/by-slug/`, `/boats/<slug>/` detail with WhatsApp/copy-link share (spec §29.7) and LISTING InquiryForm, cards link to detail, `/boats/` navigable from messaging. Public `/brokers/` and `/brokers/<slug>/` (ACTIVE only, broker-filtered cards, BROKER InquiryForm). Not built: responsive QA pass. |
| 21 | Data migration, redirects and SEO | 5, 11, 20 | **partial** | (no plan doc) | Slug backfill migration, sitemap lists boats, robots.txt. `manage.py reconcile_listings` reconciliation report. Not built: legacy `/brokers/profile/?id=` redirect (no legacy ids exist), hreflang. |
| 22 | Security, privacy, performance and observability | all features | **partial** | (no plan doc) | Fail-closed prod settings (wsgi/asgi default prod, hosts required, HSTS, proxy header) and baseline frontend headers done. CSP ships Report-Only (enforce after reports are clean). Not built: enforcing CSP, privacy/retention review, load/perf pass, observability dashboards. |
| 23 | Automated QA and UAT | all prior | **partial** | (no plan doc) | Backend lifecycle acceptance (`test_phase_23_lifecycle.py`), throttle-coverage and prod-settings tests. Not built: browser E2E, axe/responsive sweep, UAT with staff. |
| 24 | Deployment, rollback and post-launch verification | 23 | **partial** | `docs/DEPLOYMENT.md` | Runbook written (env, sequence, rollback). Not exercised against staging. |

## Execution waves

**Wave 1 (current):** plan Phases 2, 3, 4, 8 in parallel (2026-09-17).
**Wave 2 (next):** once Phase 2 plan is reviewed and merged as a plan doc,
begin Phase 2 implementation. Once Phase 3 plan is ready, begin Phase 3
implementation (can overlap with Phase 2's implementation only for the
parts of Phase 3 that don't literally need Phase 2's code merged yet —
default to sequencing 2 before 3 unless a specific Phase 3 task is proven
independent). Phase 8 implementation can start in parallel with Phase 3
as soon as Phase 2 is merged, since Phase 8 only depends on Phase 2.
**Wave 3:** once Phase 3 is merged, Phase 4 implementation can start
(and Phase 4's plan can be finalized/corrected against the real merged
Phase 3 code rather than assumptions, if anything drifted).

## Known cross-phase risk

`backend/config/settings/base.py`'s `INSTALLED_APPS` list and
`backend/config/urls.py` are shared files every new domain app must touch.
Concurrent phases both landing new apps in the same wave will each add one
line/include — expect a trivial rebase on the second merge, not a real
conflict, as long as each phase's implementer only appends its own app
without reordering or reformatting the existing list.
